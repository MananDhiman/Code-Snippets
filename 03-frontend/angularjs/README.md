# Basic Commands
```sh
npm install -g @angular/cli # install cli

ng new `angular-todo` --no-standalone # create project
ng serve --o # build and open project

ng g class `Todo` --type=model # create class / model
ng g s `services/todo` # create service for http
ng g c `todo-list` # create html component

npm install bootstrap jquery --save
```